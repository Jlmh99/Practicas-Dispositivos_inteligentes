name: shared_ble
description: UUIDs y codecs de payload BLE compartidos por wearable_app y telefono_app.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.0

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
